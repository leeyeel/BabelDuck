

// messasge types: 
// text, audio, suggested_answer(apply callback)

// message ability
    // how to render itself
    // whether to display itself in the message list
    // how to represent itself in chat completion api request body, and whether to join the completion request
